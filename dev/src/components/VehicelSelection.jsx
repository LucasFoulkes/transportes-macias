export default function VehicelSelection() {
  // i guess im getting the images from the server? yeah that makes the most sense fuck .
  // i want to download them one by one and load them one by one
  const [vehicles, setVehicles] = useState([]);
  return <div>{/* show all the vehicles available */}</div>;
}
